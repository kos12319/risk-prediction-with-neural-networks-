extends: ./agnostic_time

split:
  cv:
    enabled: true
    n_folds: 5
    mode: expanding
    initial_train_fraction: 0.4
    validation_fraction: 0.2
    gap: 0
    shuffle_within_folds: false
    train_full_after: true

